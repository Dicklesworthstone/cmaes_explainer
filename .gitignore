.vercel
.next
node_modules
*.log
*.tmp
dist
coverage
vendor/wasm_cmaes/target

# Local read-only vendored clone (standalone git checkout, not a submodule — see commit msg)
vendor/wasm_cmaes/

# Build/dependency artifacts that must never be tracked (see AGENTS.md bun-only rule)
package-lock.json
yarn.lock
pnpm-lock.yaml
tsconfig.tsbuildinfo
ios/build/
ios/Engine/
ios/EngineWeb/out/

# Local browser-smoke evidence is retained between runs but not committed.
.env*

# bv (beads viewer) local config and caches
.bv/

# Ephemeral audit/scratch artifacts (overflow-audit-*, vision-qa-*, ui-smoke, g1fix)
/tmp/
