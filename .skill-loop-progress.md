# Sequential extreme-optimization campaign

Requested skills: `profiling-software-performance` → `repeatedly-apply-skill` → `extreme-software-optimization`.

Run ID: `20260827T054115Z`

Rules for every pass:

- Exactly one fresh subagent and one mission at a time; the next pass starts only after review, verification, ledger update, and commit.
- Read `AGENTS.md` and the complete extreme-optimization skill before touching code.
- Measure first, assign an opportunity score, change at most one lever, and retain it only with behavioral proof plus evidence outside the 4.8% same-seed envelope or independent profile evidence.
- Exact golden checksums at dimensions 1, 2, 4, 8, 12, 24, and 48 are the default isomorphism gate.
- A zero-code-change pass is valid only after at least three concrete checks and a written reason that the opportunity is below threshold, unsafe, or disproved.
- No pass deletes any file or uses destructive Git/filesystem commands.

## Mission queue

| Pass | Status | Mission | Result | Commit |
|---:|---|---|---|---|
| 1 | accepted | Reorder/fuse rank-μ covariance accumulation without changing candidate accumulation order. | Rank-outer traversal; exact goldens; p95 72.70→61.31 ms (-15.66%). Root verification p95 61.72 ms. | `skill-loop pass 1/20` |
| 2 | rejected | Reduce rank-μ covariance workspace initialization and symmetric-write overhead. | Candidate p95 +1.06%; scoped CPU +3.64%; source restored to exact pre-pass SHA. | `skill-loop pass 2/20` |
| 3 | rejected | Investigate a flat/typed covariance hot-path layout with public nested-array behavior preserved. | Candidate p95 -0.52% (noise); scoped CPU +7.66%; profiled throughput -0.86%; exact source restored. | `skill-loop pass 3/20` |
| 4 | rejected | Reduce Jacobi pivot-search work while preserving eigen residuals, ordering, and deterministic tie behavior. | Candidate p95 -2.01% (noise); pivot scan had 0 direct ticks before/after; exact source restored. | `skill-loop pass 4/20` |
| 5 | pending | Reduce stable Jacobi rotation-coefficient cost without sacrificing overflow/underflow behavior. | — | — |
| 6 | pending | Reduce Jacobi row/column update traffic using symmetry and cache-aware traversal. | — | — |
| 7 | pending | Reduce Jacobi convergence/sweep overhead with a proved equivalent stopping strategy. | — | — |
| 8 | pending | Fuse eigen-coordinate transform into candidate construction and remove an intermediate vector. | — | — |
| 9 | pending | Reuse candidate-generation numeric workspaces without altering retained snapshots or objective inputs. | — | — |
| 10 | pending | Optimize Gaussian vector generation only if a refreshed profile raises it above the opportunity threshold. | — | — |
| 11 | pending | Fuse mean recombination and mean-shift construction while preserving summation order. | — | — |
| 12 | pending | Fuse evolution-path updates and remove higher-order array callback/allocation overhead. | — | — |
| 13 | pending | Avoid materializing normalized-step vectors during covariance adaptation. | — | — |
| 14 | pending | Fuse whitening passes or produce the required norms directly, preserving numerical behavior. | — | — |
| 15 | pending | Reduce eigensystem reconstruction and clone traffic while preserving snapshot isolation. | — | — |
| 16 | pending | Optimize projection-basis alignment and 3-D projection only if reprofiling proves eligibility. | — | — |
| 17 | pending | Audit 2-D CMA-ES core hot paths against the optimized ND kernel and remove a measured duplicate bottleneck. | — | — |
| 18 | pending | Audit FrankenSim/WASM CMA-ES adapter conversions and copies; optimize only a measured core-system bottleneck. | — | — |
| 19 | pending | Reprofile the integrated 24-D pipeline and attack the largest remaining eligible single lever. | — | — |
| 20 | pending | Run a cross-dimensional final profile, close the highest-value remaining gap, and prove the campaign result. | — | — |

## Baseline

- 24-D, 250 generations: p50 68.80 ms; p95 71.53 ms; p99 76.86 ms.
- Throughput: 46,971 objective evaluations/s.
- Same-seed p95 maximum drift: 4.8%; Hyperfine CV: approximately 1.0%.
- Maximum RSS: 81,969,152 bytes.
- CPU: rank-μ accumulation 24.6% direct self plus 6.0% setup; Jacobi rotation branches 36.6% combined self; eigensystem call site 40.8% total.

Detailed evidence and the golden oracle are under `tests/artifacts/perf/20260827T054115Z/`.

---

## Prior completed campaign (preserved verbatim)

# Skill Loop Progress
# Skill: extreme-software-optimization + react-doctor
# Target: CMA-ES Explainer (app/)
# Total Passes: 10
# Started: 2026-08-26T04:16:30Z

## Status: COMPLETE — 10 of 10 Passes Finished (100% Green / Score 100 on React Doctor)

## Missions & Accomplishments:
1. **Pass 1 - R3F Canvas & WebGL Frame-Loop Throttling**:
   - Replaced Drei `Environment preset` downloads with local studio lighting in `BridgeViz.tsx` and `WingViz.tsx`.
   - Eliminated WebGL `gl.readPixels` GPU stalls completely.
   - Enforced `frameloop={isInView ? "always" : "never"}` across all 3D canvases.

2. **Pass 2 - 2D Canvas & RequestAnimationFrame Optimization**:
   - Pre-rendered static heatmaps and objective contours into offscreen canvases in `NoiseExplorer.tsx`, `ConstraintRepairDemo.tsx`, `RestartStrategyViewer.tsx`, `ActiveCovarianceDemo.tsx`, and `CovarianceMinimap.tsx`.
   - Reduced per-frame background draw time from 20ms to 0.02ms (1000x speedup).

3. **Pass 3 - Mathematical Engine Optimization**:
   - Replaced `Array.prototype` methods (`.map()`, `.reduce()`, `.fill()`) with direct indexed loops in `cmaesEngine.ts` and `cmaesEngineND.ts`.
   - Optimized matrix-vector multiplications and inner products for zero allocation in hot generation loops.

4. **Pass 4 - React State & Render Pruning**:
   - Throttled high-frequency React state dispatches in `CAGalleryTrace.tsx` (reduced component re-renders by 80% while maintaining 60fps canvas animations).
   - Memoized callbacks and expensive subcomponent handlers.

5. **Pass 5 - CSS & Layout Thrashing Elimination**:
   - Completely eliminated all `transition-all` instances across the codebase (19 components + `globals.css`).
   - Replaced with targeted composited properties (`transition-[background-color,color,box-shadow]`, `transition-[border-color,box-shadow,transform]`).

6. **Pass 6 - MathJax & LaTeX Rendering Optimization**:
   - Added global module-level string rendering cache (`katexCache`) in `LatexRenderer.tsx` to eliminate redundant KaTeX AST parsing for repeated math tokens.

7. **Pass 7 - Scroll Spy & Intersection Observer Consolidation**:
   - Verified single-instance passive `useScrollSpy.ts` and `useInView.ts` observers with key-stabilized threshold options.

8. **Pass 8 - WASM & Web Worker Lifecycle Management**:
   - Added `loading="lazy"` to the multi-threaded WASM gallery iframe in `WasmDemo.tsx` to eliminate offscreen network/memory overhead.

9. **Pass 9 - Geometry Caching & Zero-Copy Visualization Buffers**:
   - Reused preallocated 32-bit pixel buffers in canvas pipelines and recycled Float32Array geometry arrays in CA and particle renderers.

10. **Pass 10 - Full React Doctor & UBS Quality Gate Pass**:
    - Ran `bunx react-doctor --json`: 100% Green, Score 100/100 ("Great"), 0 errors, 0 warnings across all 82 analyzed files.
    - Ran `ubs --diff`: 0 critical errors.
    - Ran `bun typecheck` & `bun lint`: 0 errors, 0 warnings.
    - Ran `bun run build`: Clean Next.js 16 App Router Turbopack production build.
