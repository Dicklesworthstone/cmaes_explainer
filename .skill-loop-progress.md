# Skill Loop Progress
# Skill: extreme-software-optimization + react-doctor
# Target: CMA-ES Explainer (app/)
# Total Passes: 10
# Started: 2026-08-26T04:16:30Z

## Status: IN PROGRESS — Pass 1 of 10

## Missions
1. R3F Canvas & WebGL Frame-Loop Throttling: Pause off-screen 3D canvas render loops, throttle framerates when idle, share geometries/materials across Three.js scenes.
2. 2D Canvas & RequestAnimationFrame Optimization: De-duplicate 2D canvas draw loops, cache gradient assets, dirty-rect/condition checks before canvas clear and redraws.
3. Mathematical Engine Optimization: Optimize hot CMA-ES math loops (`cmaesEngine.ts`, `cmaesEngineND.ts`, `frankensimCmaes.ts`, `cmaesEquations.ts`) with typed arrays, preallocated buffers, and fast matrix operations.
4. React State & Render Pruning: Eliminate wasteful re-renders with `React.memo`, stable callbacks (`useCallback`), memoized computed properties (`useMemo`), and state colocation.
5. CSS & Layout Thrashing Elimination: Replace all remaining `transition-all` instances with targeted transitions (`transition-colors`, `transition-transform`), add GPU acceleration (`transform-gpu`, `will-change: transform`), and strict containment.
6. MathJax & LaTeX Rendering Optimization: Streamline KaTeX / MathJax rendering with memoization and client-side equation caching to prevent DOM churn during scroll.
7. Event Listeners & Intersection Observer Unification: Consolidate scroll and resize observers into passive, single-instance handlers across `useScrollSpy.ts` and `useInView.ts`.
8. WASM & Web Worker Lifecycle Management: Lazy-load and isolate WASM execution, throttle iframe message passing, and prevent memory leaks.
9. Geometry Caching & Zero-Copy Visualization Buffers: Cache static SVG/canvas paths and implement zero-copy particle buffer updates in BridgeViz, WingViz, and TransformerViz.
10. Full React Doctor & UBS Quality Gate Pass: Run React Doctor and UBS across every component and lib file, resolving all diagnostics to achieve 100% green status.

## Completed Passes
(none yet)
