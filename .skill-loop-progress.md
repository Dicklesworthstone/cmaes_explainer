# Sequential WASM extreme-optimization campaign

Requested skills: `profiling-software-performance` -> `repeatedly-apply-skill` -> `extreme-software-optimization`.

Run ID: `20260827T161800Z`

Status: **IN PROGRESS — 17 of 20 passes verified.**

Target: the canonical `fs-cmaes-viz-wasm 0.3.0` implementation in
`/Users/jemanuel/projects/frankensim/crates/fs-cmaes-viz-wasm`, plus its exact
generated browser artifact in this repository.

Rules for every pass:

- Exactly one fresh subagent and one mission at a time; the next pass starts only after root review, verification, ledger update, and commit.
- Read both repositories' `AGENTS.md` files and the complete extreme-optimization skill before touching code.
- Measure the production WASM artifact first, change at most one lever, and retain it only with correctness proof plus a repeatable improvement outside the same-seed noise envelope or independently attributed profile evidence.
- The reference-differential test, all native kernel tests, browser artifact version, and deterministic output checksum are mandatory isomorphism gates.
- A zero-code-change pass is valid only after at least three concrete checks and a written reason that the opportunity is below threshold, unsafe, or disproved.
- No pass deletes any file or uses destructive Git/filesystem commands.

## Mission queue

| Pass | Status | Mission | Result | Commit |
|---:|---|---|---|---|
| 1 | accepted | Attack the highest-confidence serialization/float-formatting bottleneck. | Direct streaming serializer; exact envelope hashes; max-workload p95 -14.3%. | `skill-loop wasm pass 1/20` |
| 2 | accepted | Reprofile production WASM and attack the highest remaining eligible lever. | Reused exact-order matrix-vector buffers; exact hashes; root max-workload mean -0.96%. | `skill-loop wasm pass 2/20` |
| 3 | rejected | Reprofile production WASM and attack the highest remaining eligible lever. | Cached normalized steps looked faster in debug profile but max p95 regressed 4.65%; source restored exactly. | `skill-loop wasm pass 3/20` |
| 4 | rejected | Reprofile production WASM and attack the highest remaining eligible lever. | Unstable ranking preserved output but max mean/p95 regressed; source restored exactly. | `skill-loop wasm pass 4/20` |
| 5 | accepted | Reprofile production WASM and attack the highest remaining eligible lever. | Precomputed ELLI axis scales; exact hashes; root max p95 -2.90%. | `skill-loop wasm pass 5/20` |
| 6 | rejected | Reprofile production WASM and attack the highest remaining eligible lever. | Direct matvec loop had mixed tails, mean +0.25%, and failed strict Clippy; restored exactly. | `skill-loop wasm pass 6/20` |
| 7 | rejected | Reprofile production WASM and attack the highest remaining eligible lever. | Reused generation buffers were neutral, max p99 +6.28%, binary +280 B; restored exactly. | `skill-loop wasm pass 7/20` |
| 8 | rejected | Reprofile production WASM and attack the highest remaining eligible lever. | In-place rank permutation was neutral, max p95/p99 worse, binary +304 B; restored exactly. | `skill-loop wasm pass 8/20` |
| 9 | rejected | Reprofile production WASM and attack the highest remaining eligible lever. | Elite-flag formatting shrank locally but mixed cross-workload timing and +29 B binary; restored exactly. | `skill-loop wasm pass 9/20` |
| 10 | rejected | Reprofile production WASM and attack the highest remaining eligible lever. | Covariance triangle sharing had mixed tails, bounded p99 +11.61%, binary +172 B; restored exactly. | `skill-loop wasm pass 10/20` |
| 11 | accepted | Eliminate the multi-megabyte JSON boundary identified by the cross-engine profile. | Versioned packed `Float64Array` ABI; bitwise state parity; UI/max end-to-end WASM mean -31.3%/-37.9%. | `skill-loop wasm pass 11/20` |
| 12 | rejected | Reprofile the packed viewer adapter and remove its highest-frequency allocation. | Tuple-reuse candidate preserved exact state but was mixed/noisy; UI adapter mean regressed 3.90%; source restored exactly. | `skill-loop wasm pass 12/20` |
| 13 | rejected | Attribute packed construction/transfer and test one remaining Rust matrix lever. | Packet/transfer were <1%; fused sqrt/inv-sqrt matrices were bit-identical but maximum trimmed mean regressed 1.97%; restored. | `skill-loop wasm pass 13/20` |
| 14 | rejected | Reduce deterministic Gaussian-fill control-flow overhead. | Indexed-pair fill was word-identical but neutral, enlarged WASM 537 B, and worsened p99 tails; restored exactly. | `skill-loop wasm pass 14/20` |
| 15 | rejected | Remove redundant covariance reconstruction from phase-space projection. | Retained covariances were exact but the repeat was neutral/mixed and added a 64 KiB memory page; restored. | `skill-loop wasm pass 15/20` |
| 16 | rejected | Reuse caller-owned Jacobi eigensolver workspace to remove allocation and initialization overhead. | Exact output, but UI trimmed mean regressed 1.42–1.53%, maximum gains stayed below noise, and WASM grew 455 B; restored. | `skill-loop wasm pass 16/20` |
| 17 | accepted | Share deterministic trigonometric range reduction for complete Box–Muller pairs. | Paired `sin_cos` was bit-exact, UI mean 2.35–3.09% faster, maximum mean 5.44–6.28% faster, and WASM 153 B smaller. | `skill-loop wasm pass 17/20` |
| 18 | pending | Reprofile production WASM and attack the highest remaining eligible lever. | — | — |
| 19 | pending | Reprofile production WASM and attack the highest remaining eligible lever. | — | — |
| 20 | pending | Run a final cross-workload profile and close the highest remaining gap. | — | — |

## Baseline

- Bun UI-default p50/p95/p99: 6.729 / 6.931 / 7.152 ms.
- Bun admitted-maximum p50/p95/p99: 30.042 / 30.488 / 30.533 ms.
- Chromium admitted-maximum p50/p95/p99: 35.015 / 35.500 / 35.585 ms.
- Maximum envelope: 3,356,756 bytes; Bun RSS delta across 40 trials: 64,585,728 bytes.
- Named profile: envelope construction 39.1% total; optimizer 58.1%; Gaussian filling 13.3%; matrix-vector work 11.5%; Jacobi eigendecomposition 8.5%.

Detailed evidence is under `tests/artifacts/perf/20260827T161800Z/`.

Midpoint cross-engine finding: the complete UI-ready WASM path is 3.70x slower
than TypeScript on the UI default and 3.93x slower at the admitted maximum.
The remaining passes prioritize eliminating the multi-megabyte JSON boundary.

---

# Sequential extreme-optimization campaign

Requested skills: `profiling-software-performance` → `repeatedly-apply-skill` → `extreme-software-optimization`.

Run ID: `20260827T054115Z`

Status: **COMPLETE — 20 of 20 passes verified and committed.**

Rules for every pass:

- Exactly one fresh subagent and one mission at a time; the next pass starts only after review, verification, ledger update, and commit.
- Read `AGENTS.md` and the complete extreme-optimization skill before touching code.
- Measure first, assign an opportunity score, change at most one lever, and retain it only with behavioral proof plus evidence outside the 4.8% same-seed envelope or independent profile evidence.
- Exact golden checksums at dimensions 1, 2, 4, 8, 12, 24, and 48 are the default isomorphism gate.
- A zero-code-change pass is valid only after at least three concrete checks and a written reason that the opportunity is below threshold, unsafe, or disproved.
- No pass deletes any file or uses destructive Git/filesystem commands.

## Mission queue

| Pass | Status | Mission | Result | Commit |
|---:|---|---|---|---|
| 1 | accepted | Reorder/fuse rank-μ covariance accumulation without changing candidate accumulation order. | Rank-outer traversal; exact goldens; p95 72.70→61.31 ms (-15.66%). Root verification p95 61.72 ms. | `skill-loop pass 1/20` |
| 2 | rejected | Reduce rank-μ covariance workspace initialization and symmetric-write overhead. | Candidate p95 +1.06%; scoped CPU +3.64%; source restored to exact pre-pass SHA. | `skill-loop pass 2/20` |
| 3 | rejected | Investigate a flat/typed covariance hot-path layout with public nested-array behavior preserved. | Candidate p95 -0.52% (noise); scoped CPU +7.66%; profiled throughput -0.86%; exact source restored. | `skill-loop pass 3/20` |
| 4 | rejected | Reduce Jacobi pivot-search work while preserving eigen residuals, ordering, and deterministic tie behavior. | Candidate p95 -2.01% (noise); pivot scan had 0 direct ticks before/after; exact source restored. | `skill-loop pass 4/20` |
| 5 | accepted | Reduce stable Jacobi rotation-coefficient cost without sacrificing overflow/underflow behavior. | Exact-tail hypot bypass; focused throughput +5.53%, native hypot self -22.74%; exact goldens. Root p95 61.65 ms. | `skill-loop pass 5/20` |
| 6 | accepted | Reduce Jacobi row/column update traffic using symmetry and cache-aware traversal. | Canonical upper-triangle updates; focused throughput +14.38%; Jacobi self ~-7.77%; exact goldens. Root p95 60.84 ms. | `skill-loop pass 6/20` |
| 7 | rejected | Reduce Jacobi convergence/sweep overhead with a proved equivalent stopping strategy. | Removed 13.96% modeled sweeps, but p95 -1.80% (noise), focused +0.35%, profiled -0.51%; exact source restored. | `skill-loop pass 7/20` |
| 8 | rejected | Fuse eigen-coordinate transform into candidate construction and remove an intermediate vector. | Exact candidate regressed p95 +6.61% and throughput -4.87%; source restored. | `skill-loop pass 8/20` |
| 9 | rejected | Reuse candidate-generation numeric workspaces without altering retained snapshots or objective inputs. | Avoided 974,700 arrays, but p95 -1.32% (noise), profiled throughput -1.84%, heap-instrumented -0.94%; restored. | `skill-loop pass 9/20` |
| 10 | no-change | Optimize Gaussian vector generation only if a refreshed profile raises it above the opportunity threshold. | Current share 2.0% total/1.3% direct; score 1.5 below threshold; no exact eligible lever. | `skill-loop pass 10/20` |
| 11 | accepted | Fuse mean recombination and mean-shift construction while preserving summation order. | Final-rank fusion; focused p95 -17.95%, throughput +21.87%; exact goldens. Root p95 59.20 ms. | `skill-loop pass 11/20` |
| 12 | accepted | Fuse evolution-path updates and remove higher-order array callback/allocation overhead. | In-place indexed paths + fused norm; focused p95 -65.55%, throughput +180.70%; exact goldens. Root p95 57.83 ms. | `skill-loop pass 12/20` |
| 13 | rejected | Avoid materializing normalized-step vectors during covariance adaptation. | Candidate p95 +13.94%, throughput -11.39%, rank-μ self +35.41%; exact source restored. | `skill-loop pass 13/20` |
| 14 | accepted | Fuse whitening passes or produce the required norms directly, preserving numerical behavior. | Specialized negative-rank Mahalanobis² kernel; focused p95 -28.42%, throughput +47.98%, integrated call site -32.61%; exact goldens. Root p95 57.16 ms. | `skill-loop pass 14/20` |
| 15 | accepted | Reduce eigensystem reconstruction and clone traffic while preserving snapshot isolation. | Removed a redundant private projection-basis clone; focused p95 -20.75%, cloneMatrix profile -40.04%, paired RSS -4.07%; 4,176 ownership checks. Root p95 56.78 ms. | `skill-loop pass 15/20` |
| 16 | rejected | Optimize projection-basis alignment and 3-D projection only if reprofiling proves eligibility. | Projection was only 0.8% integrated; fused candidate p95 -0.22% (noise) and profile did not confirm attribution despite focused -64.93%; exact source restored. | `skill-loop pass 16/20` |
| 17 | accepted | Audit 2-D CMA-ES core hot paths against the optimized ND kernel and remove a measured duplicate bottleneck. | Reused sampler-owned Gaussian tuple as candidate z; integrated p95 -22.03%/-9.01%, focused p95 -8.46%; 30,255 parity/ownership checks exact. | `skill-loop pass 17/20` |
| 18 | rejected | Audit FrankenSim/WASM CMA-ES adapter conversions and copies; optimize only a measured core-system bottleneck. | Shipped 0.2.0 kernel correctly fails compatibility and uses TS fallback; exact tuple-fusion candidate regressed p95 ~71% and was restored; 24,104 contract checks. | `skill-loop pass 18/20` |
| 19 | accepted | Reprofile the integrated 24-D pipeline and attack the largest remaining eligible single lever. | Split Jacobi canonical-upper traversal into three fixed address ranges; focused p95 -13.55%, Jacobi self -26.43%, exact goldens. Root p95 52.49 ms. | `skill-loop pass 19/20` |
| 20 | rejected | Run a cross-dimensional final profile, close the highest-value remaining gap, and prove the campaign result. | Exact abs(apq)-reuse candidate regressed p95 +1.47% and was restored; final p95 52.71 ms (-26.31%), throughput +35.17%, exact cross-dimensional goldens. Root p95 53.98 ms. | `skill-loop pass 20/20` |

## Baseline

- 24-D, 250 generations: p50 68.80 ms; p95 71.53 ms; p99 76.86 ms.
- Throughput: 46,971 objective evaluations/s.
- Same-seed p95 maximum drift: 4.8%; Hyperfine CV: approximately 1.0%.
- Maximum RSS: 81,969,152 bytes.
- CPU: rank-μ accumulation 24.6% direct self plus 6.0% setup; Jacobi rotation branches 36.6% combined self; eigensystem call site 40.8% total.

Detailed evidence and the golden oracle are under `tests/artifacts/perf/20260827T054115Z/`.

---

## Prior completed campaign (preserved verbatim)

# Skill Loop Progress
# Skill: extreme-software-optimization + react-doctor
# Target: CMA-ES Explainer (app/)
# Total Passes: 10
# Started: 2026-08-26T04:16:30Z

## Status: COMPLETE — 10 of 10 Passes Finished (100% Green / Score 100 on React Doctor)

## Missions & Accomplishments:
1. **Pass 1 - R3F Canvas & WebGL Frame-Loop Throttling**:
   - Replaced Drei `Environment preset` downloads with local studio lighting in `BridgeViz.tsx` and `WingViz.tsx`.
   - Eliminated WebGL `gl.readPixels` GPU stalls completely.
   - Enforced `frameloop={isInView ? "always" : "never"}` across all 3D canvases.

2. **Pass 2 - 2D Canvas & RequestAnimationFrame Optimization**:
   - Pre-rendered static heatmaps and objective contours into offscreen canvases in `NoiseExplorer.tsx`, `ConstraintRepairDemo.tsx`, `RestartStrategyViewer.tsx`, `ActiveCovarianceDemo.tsx`, and `CovarianceMinimap.tsx`.
   - Reduced per-frame background draw time from 20ms to 0.02ms (1000x speedup).

3. **Pass 3 - Mathematical Engine Optimization**:
   - Replaced `Array.prototype` methods (`.map()`, `.reduce()`, `.fill()`) with direct indexed loops in `cmaesEngine.ts` and `cmaesEngineND.ts`.
   - Optimized matrix-vector multiplications and inner products for zero allocation in hot generation loops.

4. **Pass 4 - React State & Render Pruning**:
   - Throttled high-frequency React state dispatches in `CAGalleryTrace.tsx` (reduced component re-renders by 80% while maintaining 60fps canvas animations).
   - Memoized callbacks and expensive subcomponent handlers.

5. **Pass 5 - CSS & Layout Thrashing Elimination**:
   - Completely eliminated all `transition-all` instances across the codebase (19 components + `globals.css`).
   - Replaced with targeted composited properties (`transition-[background-color,color,box-shadow]`, `transition-[border-color,box-shadow,transform]`).

6. **Pass 6 - MathJax & LaTeX Rendering Optimization**:
   - Added global module-level string rendering cache (`katexCache`) in `LatexRenderer.tsx` to eliminate redundant KaTeX AST parsing for repeated math tokens.

7. **Pass 7 - Scroll Spy & Intersection Observer Consolidation**:
   - Verified single-instance passive `useScrollSpy.ts` and `useInView.ts` observers with key-stabilized threshold options.

8. **Pass 8 - WASM & Web Worker Lifecycle Management**:
   - Added `loading="lazy"` to the multi-threaded WASM gallery iframe in `WasmDemo.tsx` to eliminate offscreen network/memory overhead.

9. **Pass 9 - Geometry Caching & Zero-Copy Visualization Buffers**:
   - Reused preallocated 32-bit pixel buffers in canvas pipelines and recycled Float32Array geometry arrays in CA and particle renderers.

10. **Pass 10 - Full React Doctor & UBS Quality Gate Pass**:
    - Ran `bunx react-doctor --json`: 100% Green, Score 100/100 ("Great"), 0 errors, 0 warnings across all 82 analyzed files.
    - Ran `ubs --diff`: 0 critical errors.
    - Ran `bun typecheck` & `bun lint`: 0 errors, 0 warnings.
    - Ran `bun run build`: Clean Next.js 16 App Router Turbopack production build.
